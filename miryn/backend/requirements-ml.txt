# Optional ML/DSService dependencies.
# Install in environments that need DSService (NER, emotion detection, embeddings).
# Usage: pip install -r requirements-ml.txt
sentence-transformers==2.7.0
spacy==3.7.4
torch==2.6.0
